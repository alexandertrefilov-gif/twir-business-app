node_modules/
.next/
.next-dev/
out/
.env
.env.local
.env.*.local
storage/
coverage/
.DS_Store
*.tsbuildinfo
# Lokale Backups/Dumps/Rekonziliations-Artefakte (enthalten reale DB-Dumps)
output/

# Deployment-Secrets (echte Werte, nie committen — nur die .example-Dateien
# unter deploy/ sind zum Einchecken gedacht)
deploy/.env.production
deploy/secrets/
