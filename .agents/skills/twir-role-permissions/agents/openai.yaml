interface:
  display_name: "TWIR Role Permissions"
  short_description: "Rollen und Berechtigungen prüfen"
  default_prompt: "Use $twir-role-permissions to review the relevant TWIR project area against the current code."
