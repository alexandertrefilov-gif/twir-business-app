interface:
  display_name: "TWIR Calculation Integrity"
  short_description: "Berechnung und Rundung prüfen"
  default_prompt: "Use $twir-calculation-integrity to review the relevant TWIR project area against the current code."
