<!-- AGENT-META
{
  "id": "REQ-015.2-A",
  "title": "REQ-015 Fixture-Regression in gga-cabinet-inspection-db.test.ts",
  "status": "COMPLETED",
  "priority": "P1",
  "type": "bugfix",
  "scope": "single",
  "files_allowed": ["tests/integration/gga-cabinet-inspection-db.test.ts"],
  "files_protected": [
    "lib/services/collaboration-phase2.service.ts",
    "lib/services/gga-cabinet.service.ts",
    "lib/collaboration/cabinet-workflow.ts",
    "lib/collaboration/project-workflow.ts"
  ],
  "tests_required": ["integration", "db"],
  "db_test_required": true,
  "playwright_required": false,
  "browser_qa_required": false,
  "manual_approval_required": true,
  "report_format": "default",
  "created": "2026-09-18",
  "max_repair_attempts": 3,
  "repair_attempts": 0,
  "archived_at": "2026-09-19T00:00:00.000Z",
  "archive_reason": "Bereits vor Einführung der Pipeline implementiert und real verifiziert (siehe Bericht REQ-015.2 im vorangegangenen Auftrag). Aus der aktiven Queue entfernt und archiviert — nicht ausführbar über agent:next."
}
-->
> **Status:** COMPLETED (archiviert) · **Priority:** P1 · **Type:** bugfix · **Repair-Versuche:** 0/3

## ARCHIV-HINWEIS

Dieser Task wurde NICHT über die Pipeline ausgeführt. Er ist ein
nachträglich archiviertes Beispiel für das Requirement-Format
(`docs/agent/REQUIREMENT_FORMAT.md`) — die eigentliche Arbeit ist bereits
vor Einführung der Pipeline erledigt und real verifiziert worden (10/10
Tests grün gegen `TEST_DATABASE_URL`). Aus `.agent/queue/` entfernt, damit
`npm run agent:next` diesen Task nicht mehr auswählt.

## CONTEXT (historisch)

REQ-015 hat `requestCollaborationApproval()` serverseitig an eine
vollständige ABNAHME-Checkliste gekoppelt
(`ggaCabinetBereitFuerInterneFreigabe`). Die Fixtures in
`gga-cabinet-inspection-db.test.ts` (4 Tests) riefen diese Funktion aber
auf, bevor die Checkliste vollständig war — vor REQ-015 unproblematisch,
seither ein durch den vollständigen `tests/integration/`-Lauf aufgedeckter
Regressionsfund (nicht in den ursprünglich für REQ-015.1 benannten
Dateien enthalten).

## ACCEPTANCE_CRITERIA

- [ ] Alle 4 betroffenen Tests wieder grün, ohne den ursprünglichen
      Testzweck zu verändern.
- [ ] Ausschließlich über bestehende produktive Service-Funktionen
      hergestellt (`setGgaCabinetInspectionItem`), keine direkte
      DB-Manipulation.
- [ ] Keine der in `files_protected` genannten Dateien verändert.

## TESTS_REQUIRED

- `tests/integration/gga-cabinet-inspection-db.test.ts` real gegen
  `TEST_DATABASE_URL`.

## KNOWN_ISSUES

- Die 5 REQ-015-Gate-Funktionen dürfen unter keinen Umständen
  abgeschwächt werden, um einen Test grün zu bekommen — in diesem Fall:
  STOP und berichten statt implementieren.

## DO_NOT

- Keine Business-Logik ändern.
- Keine REQ-015-Gates abschwächen.

## REPORT_FORMAT

default (siehe `docs/agent/DEVELOPMENT_PIPELINE.md` Abschnitt 4).
